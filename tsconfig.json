{
	"compilerOptions": {
		"target": "ES2017" /* Установите версию языка JavaScript для создаваемого JavaScript и включите совместимые объявления библиотеки. */,
		"module": "CommonJS" /* Укажите, какой код модуля генерируется. */,
		"rootDir": "./src" /* Укажите корневую папку внутри ваших исходных файлов. */,
		"moduleResolution": "Node" /* Укажите, как TypeScript ищет файл из данного спецификатора модуля. */,
		"declaration": true /* Генерировать .d.ts файлы из файлов TypeScript и JavaScript в вашем проекте. */,
		"declarationMap": true /* Создавать карты исходников для файлов d.ts. */,
		"sourceMap": true /* Создавать файлы карт исходников для выданных файлов JavaScript. */,
		"outDir": "dist" /* Укажите папку вывода для всех выданных файлов. */,
		"noEmit": true /* Отключить выдачу файлов из компиляции. */,
		"isolatedModules": true /* Убедитесь, что каждый файл можно безопасно транспонировать, не полагаясь на другие импорты. */,
		"esModuleInterop": true /* Выдать дополнительный JavaScript для облегчения поддержки импорта модулей CommonJS. Это включает 'allowSyntheticDefaultImports' для совместимости типов. */,
		"forceConsistentCasingInFileNames": true /* Убедитесь, что регистр правильный в импортах. */,
		"strict": true /* Включить все строгие опции проверки типов. */,
		"noImplicitAny": false /* Включить отчет об ошибках для выражений и объявлений с подразумеваемым типом 'any'. */,
		"strictNullChecks": true /* При проверке типов учитывайте 'null' и 'undefined'. */,
		"strictFunctionTypes": true /* При назначении функций проверьте, чтобы параметры и возвращаемые значения были совместимыми по подтипу. */,
		"strictBindCallApply": true /* Проверьте, чтобы аргументы для методов 'bind', 'call' и 'apply' соответствовали исходной функции. */,
		"strictPropertyInitialization": true /* Проверьте наличие свойств класса, которые объявлены, но не установлены в конструкторе. */,
		"noImplicitThis": true /* Включить отчет об ошибках, когда 'this' получает тип 'any'. */,
		"useUnknownInCatchVariables": true /* Переменные по умолчанию ловят как 'unknown', а не 'any'. */,
		"alwaysStrict": true /* Убедитесь, что 'use strict' всегда выдается. */,
		"noUnusedLocals": true /* Включить отчет об ошибках, когда локальные переменные не читаются. */,
		"noUnusedParameters": true /* Вызовите ошибку, когда параметр функции не читается. */,
		"skipLibCheck": true,
		"baseUrl": ".",
		"paths": {
			"@/*": ["./src/*"]
		}
	},
	"include": ["src", "node_modules/@types"]
}
